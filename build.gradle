buildscript {
    repositories {
        mavenCentral()
		mavenLocal()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
        maven {
            name = "sonatype"
            url = "https://oss.sonatype.org/content/repositories/snapshots/"
        }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:1.2-SNAPSHOT'
    }
}

apply plugin: 'forge'

minecraft {
    version = "1.7.10-10.13.2.1291"
}

group = 'modmuss50.network'
archivesBaseName = "HardCoreMapReset"

def ENV = System.getenv()
if (ENV.BUILD_NUMBER) {
    version = "ALPHA-2.0." + "${System.getenv().BUILD_NUMBER}"
} else {
    version = "ALPHA-2.0.0"
}
