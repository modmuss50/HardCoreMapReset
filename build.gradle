buildscript {
    repositories {
        mavenCentral()
		mavenLocal()
        maven {
            name = "forge"
            url = "http://files.minecraftforge.net/maven"
        }
        maven {
            name = "sonatype"
            url = "https://oss.sonatype.org/content/repositories/snapshots/"
        }
    }
    dependencies {
        classpath 'net.minecraftforge.gradle:ForgeGradle:1.2-SNAPSHOT'
    }
}

apply plugin: 'forge'

minecraft {
    version = "1.8-11.14.1.1419"
    mappings = "snapshot_20141130"
}

group = 'modmuss50.network'
archivesBaseName = "HardCoreMapReset"

def ENV = System.getenv()
if (ENV.BUILD_NUMBER) {
    version = "1.8-2.0." + "${System.getenv().BUILD_NUMBER}"
} else {
    version = "1.8-2.0.0"
}
